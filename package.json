{
  "name": "battle-api",
  "version": "1.0.0",
  "description": "Api for fetching battle data",
  "main": "index.js",
  "scripts": {
    "start-local": "eslint --ignore-path .gitignore . && nodemon index.js --exec 'npm run lint && node'",
    "lint": "eslint --ignore-path .gitignore .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sanket Subhash Hande",
  "license": "GPL-3.0",
  "dependencies": {
    "express": "^4.16.4"
  },
  "devDependencies": {
    "eslint": "^5.8.0",
    "eslint-plugin-node": "^8.0.0",
    "nodemon": "^1.18.5"
  }
}
